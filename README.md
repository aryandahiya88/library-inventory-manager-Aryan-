# library-inventory-manager-Aryan-

Library Inventory Manager

A simple command-line application to manage library books using Python. Tracks book status, allows searching, and saves data in JSON format.

## Features

- Add new books
- Issue and return books
- View all books
- Search books by title
- Persistent storage using `books.json`
- Simple logging of operations
